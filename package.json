{
  "private": true,
  "license": "AGPL",
  "scripts": {
    "postinstall": "npmi && jspmi",
    "start": "cd server && node main.js",
    "npmi": "cd server && npm i",
    "jspmi": "cd client && jspm i --yes",
    "sfx": "cd client && npm run sfx"
  },
  "engines": {
    "node": "^5.4.0"
  }
}
